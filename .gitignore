node_modules/
dist/
.DS_Store

# Segredos — nunca commitar
.env
.env.*
!.env.example

# Saídas de scraping e arquivos baixados dos portais
.just-scrape/
downloads/
*.xlsx
!referencias/*.xlsx
# Fixtures de teste são versionadas de propósito: os testes nunca batem no portal ao vivo
!tests/fixtures/*.xlsx

# Trigger.dev
.trigger/

# O token de push local não tem escopo `workflow`, então o arquivo de
# automação é criado/editado pela interface web do GitHub.
.github/
